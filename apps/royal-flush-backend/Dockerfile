# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build royal-flush-backend`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t royal-flush-backend`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system royal-flush-backend && \
          adduser --system -G royal-flush-backend royal-flush-backend

COPY dist/apps/royal-flush-backend royal-flush-backend
RUN chown -R royal-flush-backend:royal-flush-backend .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix royal-flush-backend --omit=dev -f install

CMD [ "node", "royal-flush-backend" ]
